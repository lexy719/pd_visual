// hero-003 — self-contained: PixelHero ships with default props.
// Requires the shadcn theme CSS vars (globals.css provides them) or it renders invisible.
import { PixelHero } from './active-component'

export default function App() {
  return <PixelHero />
}
